<form class="form-delete" action="{{ path('app_sweat_delete', { 'id': sweat.id }) }}" 
    method="post" onsubmit="return confirm('êtes vous sur de vouloir supprimer ce produit?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sweat.id) }}">
    <button class="delete-form-submit">SUPPRIMER</button>
</form>